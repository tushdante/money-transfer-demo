{
  "name": "money-transfer-e2e",
  "version": "1.0.0",
  "description": "E2E tests for money transfer demo",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "install-deps": "playwright install"
  },
  "devDependencies": {
    "@playwright/test": "^1.40.0"
  }
}
